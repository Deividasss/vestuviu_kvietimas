# Production env (used by CRA during production build, incl. Vercel)
# Leave empty so the app calls same-origin /api/rsvp (Vercel function proxy).
REACT_APP_API_BASE_URL=vestuviubackend-production.up.railway.app

# RSVP endpoint (default)
REACT_APP_RSVP_ENDPOINT=/api/rsvp

